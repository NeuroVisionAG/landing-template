# 🚀 Протокол развертывания лендингов (NeuroVision Architecture)

Этот документ фиксирует архитектуру, инструменты и процесс развертывания лендингов, чтобы в будущем не пришлось "изобретать велосипед".

## 🏗 Архитектура

Мы используем гибридный подход: современный фронтенд + надежный серверный рендеринг.

1. **Frontend**: **Next.js 15** (App Router).
    * *Почему:* Дает лучшую SEO-оптимизацию и скорость загрузки по сравнению с обычным React.
    * *Стили:* **Tailwind CSS v4** + **Shadcn UI** (компоненты) + **Framer Motion** (анимации).
    * *Контент:* Управляется через **`src/config/landing.json`** (Вайб-кодинг: меняем JSON — меняется сайт, не трогая код).

2. **Server (VPS)**:
    * **OS**: Ubuntu.
    * **Process Manager**: **PM2**.
        * *Зачем:* Держит сайт включенным 24/7, перезапускает при падении или обновлении сервера.
    * **Reverse Proxy**: **Traefik** (в Docker).
        * *Зачем:* Автоматически выдает SSL-сертификаты (HTTPS) и маршрутизирует домены (например, `land.neurovisionag.online` -> порт 3001).

3. **Deployment Flow (Workflow)**:
    * **Local**: Разработка на Windows.
    * **Sync**: Скрипт `sync_to_github.ps1` (обходит ограничения локальной среды и отправляет код на GitHub).
    * **Production**: Код скачивается с GitHub на сервер и запускается.

---

## 🛠 Как создать НОВЫЙ лендинг на этой базе

### 1. Подготовка нового проекта

Просто скопируйте всю папку `landing-template` и назовите её по-новому (например, `new-landing`).

### 2. Настройка контента

Откройте `src/config/landing.json` и поменяйте:

* Тексты, заголовки, ссылки.
* Картинки (используем внешние ссылки или кладем в `public`).
* Цветовую схему (в `globals.css` если нужно кардинально менять цвета).

### 3. Отправка на GitHub

1. Создайте **новый репозиторий** на GitHub.
2. В папке проекта откройте `sync_to_github.ps1` и поменяйте ссылку на репозиторий (строка с `git remote add origin ...`).
3. Запустите скрипт: `.\sync_to_github.ps1`.

### 4. Настройка сервера (VPS)

Заходим на сервер по SSH и выполняем:

1. **Клонируем:**

    ```bash
    cd /var/www
    git clone https://github.com/NeuroVisionAG/new-landing.git
    cd new-landing
    ```

2. **Ставим зависимости:**

    ```bash
    npm install
    npm run build
    ```

3. **Запускаем (на новом порту!):**
    Если старый лендинг был на 3001, этот ставим на **3002**.

    ```bash
    pm2 start npm --name "new-landing" -- start -- -p 3002
    pm2 save
    ```

4. **Подключаем домен (Traefik):**
    В этом проекте нет `docker-compose.yml`, мы управляем через внешний. Но чтобы Traefik увидел новый порт, нужно добавить его в общий конфиг или запустить маленький контейнер-пустышку с метками (как мы делали с `landing-template`).

---

## 🤖 Основные команды

**Локально (Windows):**

* `npm run dev` — запустить сайт для проверки (localhost:3000).
* `.\sync_to_github.ps1` — сохранить изменения и отправить в интернет.

**На сервере:**

* `git pull` — скачать обновления.
* `npm run build` — пересобрать сайт (после обновлений).
* `pm2 restart [name]` — перезапустить.
* `pm2 list` — посмотреть список запущенных сайтов.
* `docker logs n8n-traefik-1` — если проблемы с SSL/HTTPS.

---

## 💡 Почему сделано именно так? (Justification для истории)

* **Почему не просто HTML файлы?** Нам нужны компоненты (React), чтобы быстро собирать сложные блоки (FAQ, Тарифы) и иметь анимации.
* **Почему не Docker для самого сайта?** Node.js в `PM2` работает на "голом железе" чуть быстрее и проще обновляется одной командой `git pull`, без пересборки тяжелых образов.
* **Почему скрипт PowerShell?** Чтобы избежать проблем с правами и путями к Git в Windows-консоли агента.

*Автоматизация настроена так, чтобы вы могли сосредоточиться на **контенте** (JSON), а не на коде.*
